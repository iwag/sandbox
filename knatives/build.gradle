plugins {
    id 'org.jetbrains.kotlin.multiplatform' version '1.5.21'
}

repositories {
    mavenCentral()
}

kotlin {
   // macosX64('native') { // on macOS
         linuxX64('native') { // on Linux
        // mingwX64('native') // on Windows
        binaries {
            executable {
                entryPoint = 'sample.main'
            }
        }
    }
}

wrapper {
    gradleVersion = '6.7.1'
    distributionType = 'BIN'
}

//application {
//    mainClass = 'sample.SampleLinuxKt'
//}